@model OShop.ViewModels.VatEditViewModel
@using System.Globalization
@using OShop.Helpers

<fieldset>
    <div>
        @Html.LabelFor(m => m.SelectedVatId, T("VAT rate"))
        @Html.DropDownListFor(m => m.SelectedVatId, Model.VatRates.BuildVatSelectList(true, T("None").Text))
        @Html.ValidationMessageFor(m => m.SelectedVatId)
    </div>
</fieldset>
