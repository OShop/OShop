@model OShop.Models.OrderPart
@{
    if (Model.ContentItem.Id > 0) {
        Layout.Title = T("Order {0}", Model.Reference);
    }
}

<fieldset>
    @Html.LabelFor(m => m.OrderStatus)
    @Html.EnumDropDownListFor(m => m.OrderStatus)
</fieldset>