@using OShop.Models
@{
    CustomerPart customer = Model.ContentPart;
}

<div>
    @customer.FirstName @customer.LastName
    @if (Model.AllowEdit) {
        @Html.ActionLink(T("Edit").Text, "Edit", "Customer", new { area = "OShop", returnUrl = Context.Request.RawUrl }, new { })
    }
</div>