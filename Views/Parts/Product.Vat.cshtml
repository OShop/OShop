@using System.Globalization
@using OShop.Models
@using OShop.Helpers

@{
    VatRecord vat = Model.Vat;
    ProductPart product = Model.ContentPart;
}

@if (vat != null) {
    <span class="vatprice">@String.Format(Model.NumberFormat, "{0:C}", vat.GetVatIncludedPrice(product.UnitPrice))</span>
}
