@using OShop.Models

@{
    OrderPart order = Model.ContentPart;
}

@if (!string.IsNullOrWhiteSpace(order.Reference)) {
    <span class="order-ref">
        @T("Order number : {0}", order.Reference)
    </span>
}