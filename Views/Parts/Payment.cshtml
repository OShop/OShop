@using OShop.Models
@using OShop.Extensions
@{
    PaymentPart part = Model.ContentPart;
}

<div class="order-shipping">
    <h3>@T("Payment :")</h3>
    @T("Amount paid : ")@String.Format(WorkContext.NumberFormat(), "{0:C}", part.AmountPaid)
</div>